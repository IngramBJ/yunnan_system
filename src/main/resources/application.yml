server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/yunnan_system?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf8&allowPublicKeyRetrieval=true
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    open-in-view: false

jwt:
  secret: yunnan_system_secret_key_please_change_me
  expire-seconds: 86400

springdoc:
  swagger-ui:
    path: /swagger-ui.html